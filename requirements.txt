fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
aiohttp>=3.9.0
python-multipart>=0.0.9

# LLM SDK（按需安装）
openai>=1.30.0          # OpenAI / 通义千问 / 所有 OpenAI 兼容接口
anthropic>=0.26.0       # Anthropic Claude
# google-generativeai   # Google Gemini（可选）

# 微信个人号（可选，已逐步受限）
# itchat                # pip install itchat（Python 3.9以下）

# 安全/加密（飞书加密消息需要）
# pycryptodome          # pip install pycryptodome

# Telegram Bot（可选）
# python-telegram-bot>=21.0  # pip install python-telegram-bot
